# Test categories

- `unit/` covers isolated bootstrap APIs and package metadata.
- `integration/` covers installed entry points and process behavior.
- `conformance/` will contain backend-neutral accepted language behavior.
- `generative/` will contain constructive well-typed generation and drift checks.

Stage 0 intentionally has no conformance or generative program cases.

