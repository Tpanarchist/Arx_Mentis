# Generative tests

Future generators construct well-typed programs from an independent encoding of the
typing rules. They must not call the production checker to decide what to generate.
Rule changes require explicit generator drift review. Stage 0 defines no generators.

