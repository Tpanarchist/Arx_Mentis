# Examples

Examples are evidence-bearing language programs once syntax exists. Each should name
the idea it isolates, the expected observations, and the feature decisions it
depends on. Prefer the smallest program that reveals an idea; if domain decoration
obscures the idea, record that friction and add a reduced companion example.

Stage 0 contains no language examples. CLI invocations in the root README are
packaging smoke tests, not evidence about language design.

